### Some set up must have.

- To make the file executable (maybe need sudo permission)

    (sudo) chmod +x bin/parking_lot

    (sudo) chmod +x bin/run_functional_tests

    (sudo) chmod +x bin/setup
